V33.64 — Dragon Racing flight direction + animation hotfix

Files included:
- dragon-racing-race.js

Fixes:
- Corrected the Iskandar race sprite so it no longer flies backwards during the race.
- Added a race-specific Iskandar flight frame override to avoid bad/problematic flight frames.
- Added a race-specific Calvora flight loop override so a non-flying/incorrect frame no longer appears in its flying animation.

Notes:
- This is a focused hotfix only for race presentation.
- No changes to race balance, results logic, XP, audio, or UI layout.
