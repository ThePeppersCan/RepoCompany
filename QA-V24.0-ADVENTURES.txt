V24.0 — VELMORA ADVENTURES PHASE 1 QA
======================================

BACKEND / LIVE SUPABASE
PASS — additive migration applied successfully.
PASS — 19 adventure_* tables present after migration.
PASS — existing characters schema was inspected/reused by auth UUID; no existing columns altered for Adventures.
PASS — transaction-only CatAsthma test created a new Adventure, loaded it, accepted the main quest, accepted a daily job, travelled to Willowmere, completed the job, completed the starter quest, received the odd token, received server rewards and executed a d20 check.
PASS — transaction test reward ledger contained two unique reward sources.
PASS — transaction rolled back; CatAsthma retained 0 Adventure saves after QA.
PASS — second transaction used a normal account: admin flag false, adventure_admin_patch_self rejected, direct UPDATE to adventure_characters rejected by privileges.
PASS — second transaction rolled back; normal test account retained 0 Adventure saves.
PASS — reward ledger makes quest/job payout idempotent by unique source key.
PASS — Adventure reward helpers/internal state helpers are not executable by browser PUBLIC role.
PASS — client cannot directly INSERT/UPDATE/DELETE Adventure user-state tables.

FRONTEND / STATIC
PASS — adventures-v1.js parses with node --check.
PASS — Adventures CSS brace balance checked.
PASS — responsive rules exist at <=1000px, <=760px and <=480px; mobile switches side panels to full-screen drawers rather than shrinking the desktop UI.
PASS — current V23.7 index diff contains exactly two additions: adventures-v1.css and adventures-v1.js asset references.
PASS — no existing quest markup was replaced; launcher is injected into the current QUESTS list at runtime.
PASS — no Repo Sports/Reparty/TCG files are bundled in this patch.
PASS — free-text observation checks no longer persist arbitrary typed text into action history.
PASS — Enter submits the free-action input and advances the name step during character creation.

BROWSER LIMITATION
ATTEMPTED — headless Chromium against a local served copy.
The managed Chromium process did not complete within the test timeout in this environment, so this report does NOT claim a successful full live browser visual/console pass. Backend lifecycle testing is real and completed; browser rendering should be smoke-tested once after applying to the deployed site.

TEST SAVE CLEANLINESS
PASS — both accounts used in database QA show zero persistent Adventure saves after rollback. No player progression was left behind by testing.
