V33.05 — My Bedroom Keeper Controls + Hover Cleanup

Included
- removed the ugly brown hover/background behaviour on bedroom furniture hover states
- edit mode: right-click any placed furniture to instantly Put Away
- added six keeper sprite sheets for bedroom-only character avatars
- account-linked bedroom keeper chosen deterministically per username
- bedroom keeper can now be controlled with WASD / arrow keys
- bedroom keeper shows near-furniture interaction prompt and can interact with items using E
- bed interaction uses the sleeping row on the sprite sheet
- kept Main House return button and previous editor fixes

Notes
- keeper position + assigned sheet save locally with the bedroom state
- server RPC is unchanged, so bedroom furniture persistence still works exactly as before
- if you later want exact hard-mapped usernames to specific sprite sheets, that can be added easily
