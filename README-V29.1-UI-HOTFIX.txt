V29.1 — VELMORA ADVENTURES UI / INTERACTION HOTFIX

Apply over V29.0 / current Phase 7 build.

Fixes:
- Main Adventure scene/story can now scroll vertically instead of clipping lower content.
- Character creator choice panels have a real internal scroll area and keep BACK / CONTINUE visible.
- Closed drawer is now genuinely hidden instead of leaving the large dark rectangle over the scene.
- Closed drawer cannot intercept clicks.
- PEOPLE HERE NOW character cards are explicitly interactive and show TALK.
- NPC greeting path falls back safely to the existing Phase 2 greeting if the Phase 7 greeting enhancement fails.
- Phase header now correctly says PHASE 7.
- Repairs accidental regex control characters in Phase 7 free-text social matching.
- Unique v29.1 JS/CSS filenames prevent stale browser cache from keeping the broken layout.

No SQL. No backend/database changes.
No unrelated systems included.
