REPO COMPANY V18.1 — BINDER FIT + CROSS-SPREAD DRAG V5
======================================================

Fixes from the screenshot:
- Open binder is always centred and kept inside the viewport instead of being cut off.
- Hidden Cards holder remains to the right of the binder, with a reserved side gutter so it is fully visible.
- Binder itself is not horizontally moved to make room for the holder.
- Left/right click-to-turn areas stay completely invisible.
- Dragging a card to another spread works again: while dragging, move the card onto the invisible left/right binder edge and hold briefly; the binder turns to the adjacent spread while keeping the card in your drag operation.
- Existing card layout/storage data and binder styling systems are preserved.

Install:
1. Replace script.js.
2. Merge the included assets/quidditch-tcg-binder folder (same binder assets as V4).
3. No SQL/database changes are required.
