REPO SPORTS WORLD CUP 2026 — LIMITED EVENT V1.10 AUDIO + BACKGROUND HOTFIX

THIS IS A CLIENT-ONLY HOTFIX.
DO NOT re-run the V1.9 reset SQL for this update.
Your current pack/card/passport/watch state is preserved.

INSTALL
1. Replace the website's current script.js with the included script.js.
2. Copy this asset into the matching website path:
   assets/quidditch-tcg/world-cup-2026/limited-event-fullscreen-bg.png
3. Hard refresh the site (Ctrl + Shift + R).

CHANGES
- Keeps the V1.9 cinematic World Cup card opening/walkout animation.
- Pack openings are now intentionally quiet until the player-name announcement.
- Removed walkout crowd loop, country announcement, impact thud, crowd build sound,
  reveal trumpet/synth sting, goal cheer/clapping and set-complete cheer from pack reveals.
- The existing World Cup menu music is paused while the pack reveal is running and
  resumes after returning to the Pack Locker.
- The player-name MP3 remains the only reveal audio and still plays at full volume.
- National flag artwork is now constrained by BOTH width and height using object-fit: contain,
  so the full flag remains visible instead of being clipped by the reveal stage.
- The supplied stadium artwork is used full-screen as the modal backdrop behind BOTH
  the Limited Event menu and the Passport box.
- Removed the large opaque wrapper behind the two panels so the stadium actually reads
  as the page background; the menu and Passport stay readable as dark floating panels.

NO DATABASE CHANGE
- 2 free packs remain unchanged.
- Current unlocked cards remain unchanged.
- Passport tickets remain unchanged.
- Match watch progress remains unchanged.
