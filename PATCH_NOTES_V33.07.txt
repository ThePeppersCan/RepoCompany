V33.07 — Bedroom Keeper Scale, Sleep & Depth Fix

- Reduced bedroom keeper sprites by 25% from V33.06.
- Replaced placed-furniture <button> elements with neutral div interaction targets so global button hover CSS can no longer paint brown boxes behind sprites.
- Hardened transparent hover/focus/active styles for placed furniture.
- Interaction prompt now hides immediately while the keeper is using furniture.
- Bed interaction now targets the actual mattress area rather than the floor anchor.
- Sleeping sprite rotates to match the bed's isometric direction, including flipped beds.
- Keeper is drawn above bedroom furniture to prevent tall objects incorrectly clipping over the character.
- Fixed left/right idle frame mapping: left uses the true left-facing idle frame; right uses the true right-facing idle frame.
- On waking, keeper steps back to a safe point at the foot of the bed.
